# Drop these two screenshots here to complete the blog

Referenced from [../../blog-mcp-azure-container-apps.md](../../blog-mcp-azure-container-apps.md).

| File | What to capture |
|---|---|
| `portal-overview.png` | Azure Portal → your Container App (`ca-mcp-server-*`) → **Overview** tab, showing the FQDN, Running status, and the `Application Url` on the right pane. |
| `portal-logs.png` | Portal → the same Container App → **Log stream** (or **Logs** with a KQL query), showing entries like `"get_weather" completed. IsError = False`. A terminal split with server logs + agent output also works. |

Recommended: 1600–2000 px wide, PNG. Anonymize any subscription IDs or tokens.
