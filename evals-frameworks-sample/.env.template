# The agent AND the LLM-as-judge use these.
# Option A: OpenAI directly (simplest; works with both DeepEval and Inspect)
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o

# Option B: Azure OpenAI (agent side; Inspect/DeepEval judge still uses OPENAI_API_KEY)
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
# AZURE_OPENAI_API_KEY=your-key-here
# AZURE_OPENAI_DEPLOYMENT=gpt-4o

# Trials per case (Lab 10 — measure the distribution, don't sample once)
EPOCHS=5
# Set to 1 to enable the LLM-as-judge rubric metric in the DeepEval harness
# RUN_LLM_JUDGE=1
